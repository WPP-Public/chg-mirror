// src/blocks/hero-destination/lib/types.ts

export interface HeroDestinationItem {
  picture: Element;
  imageAlt: string;
  heading: string;
  /** Original row element for moveInstrumentation */
  sourceRow: HTMLElement;
}
